    <!-- ===== FOOTER ===== -->
    <footer class="border-t border-slate-200 bg-white mt-12">
      <div class="mx-auto max-w-7xl px-4 py-12">
        <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-5">
          <!-- Brand - bigger first column -->
          <div class="sm:col-span-2 lg:col-span-2">
            <a class="inline-block" href="<?php echo domain; ?>">
              <img src="assets/images/naafiun.webp" alt="Naafiun" class="h-10 w-auto" />
            </a>
            <p class="mt-4 text-base leading-7 text-slate-600">
              Naafiun is a leading book shop in Bangladesh. We offer thousands of islamic, general and academic books at a discounted price. We provide good packaging with free shipping on Bangladesh orders over BDT 2021
            </p>
            <div class="mt-5 space-y-2.5 text-base text-slate-600">
              <p class="flex items-center gap-2.5">
                <svg class="h-5 w-5 text-emerald-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/></svg>
                <a href="tel:+8801407700600" class="font-medium hover:text-emerald-600 transition">+8801407700600</a>
              </p>
              <p class="flex items-center gap-2.5">
                <svg class="h-5 w-5 text-emerald-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg>
                <a href="mailto:info@naafiun.com" class="font-medium hover:text-emerald-600 transition">info@naafiun.com</a>
              </p>
              <p class="flex items-start gap-2.5">
                <svg class="mt-0.5 h-5 w-5 shrink-0 text-emerald-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
                <span>House no. 906, Lane 02, Shatarkul Road, North Badda</span>
              </p>
            </div>
            <!-- Social Media Icons -->
            <div class="mt-5 flex gap-3">
              <a href="#" class="flex h-10 w-10 items-center justify-center rounded-full bg-blue-600 text-white hover:bg-blue-700 transition shadow-sm" aria-label="Facebook">
                <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24"><path d="M13.5 22v-8h2.7l.5-3H13.5V9.1c0-.8.3-1.4 1.5-1.4H16.9V5.1c-.3 0-1.3-.1-2.4-.1-2.5 0-4.2 1.5-4.2 4.3V11H7.7v3h2.6v8h3.2Z"/></svg>
              </a>
              <a href="#" class="flex h-10 w-10 items-center justify-center rounded-full bg-red-600 text-white hover:bg-red-700 transition shadow-sm" aria-label="YouTube">
                <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24"><path d="M21.6 7.2a3 3 0 0 0-2.1-2.1C17.7 4.6 12 4.6 12 4.6s-5.7 0-7.5.5A3 3 0 0 0 2.4 7.2 31 31 0 0 0 2 12a31 31 0 0 0 .4 4.8 3 3 0 0 0 2.1 2.1c1.8.5 7.5.5 7.5.5s5.7 0 7.5-.5a3 3 0 0 0 2.1-2.1A31 31 0 0 0 22 12a31 31 0 0 0-.4-4.8ZM10 15.5v-7l6 3.5-6 3.5Z"/></svg>
              </a>
              <a href="#" class="flex h-10 w-10 items-center justify-center rounded-full bg-gradient-to-tr from-yellow-400 via-red-500 to-purple-600 text-white hover:opacity-90 transition shadow-sm" aria-label="Instagram">
                <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24"><path d="M7.5 2h9A5.5 5.5 0 0 1 22 7.5v9A5.5 5.5 0 0 1 16.5 22h-9A5.5 5.5 0 0 1 2 16.5v-9A5.5 5.5 0 0 1 7.5 2Zm9 2h-9A3.5 3.5 0 0 0 4 7.5v9A3.5 3.5 0 0 0 7.5 20h9a3.5 3.5 0 0 0 3.5-3.5v-9A3.5 3.5 0 0 0 16.5 4ZM12 7a5 5 0 1 1 0 10 5 5 0 0 1 0-10Zm0 2a3 3 0 1 0 0 6 3 3 0 0 0 0-6Zm5.5-2.4a1.1 1.1 0 1 1 0 2.2 1.1 1.1 0 0 1 0-2.2Z"/></svg>
              </a>
              <a href="#" class="flex h-10 w-10 items-center justify-center rounded-full bg-green-500 text-white hover:bg-green-600 transition shadow-sm" aria-label="WhatsApp">
                <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347z"/></svg>
              </a>
            </div>
          </div>
          <!-- Who We Are -->
          <div>
            <h3 class="text-base font-bold text-slate-900">Who We Are</h3>
            <ul class="mt-4 space-y-3 text-sm">
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="about">About Us</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="#">Buy Wholesale</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="#">Shipping & Delivery</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="contact">যোগাযোগ করুন</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="#">শর্তাবলী</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="#">নীতিমালা</a></li>
            </ul>
          </div>
          <!-- জনপ্রিয় -->
          <div>
            <h3 class="text-base font-bold text-slate-900">জনপ্রিয়</h3>
            <ul class="mt-4 space-y-3 text-sm">
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="category">জেনারেল ও অ্যাকাডেমিক বই</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="category">ড. খোন্দকার আব্দুল্লাহ জাহাঙ্গীর এর বই</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="category">আরিফ আজাদ এর বই</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="category">প্রি-অর্ডার</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="category">প্যাকেজ</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="category">ইলেকট্রনিক্স</a></li>
            </ul>
          </div>
          <!-- প্রয়োজনীয় লিংক -->
          <div>
            <h3 class="text-base font-bold text-slate-900">প্রয়োজনীয় লিংক</h3>
            <ul class="mt-4 space-y-3 text-sm">
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="cart">শপিং কার্ট</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="#">রিফান্ড</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="cart">শপিং ব্যাগ</a></li>
              <li><a class="text-slate-600 hover:text-emerald-600 transition" href="#">কিভাবে কেনাকাটা করবেন ?</a></li>
            </ul>
          </div>
        </div>
        <div class="mt-10 flex flex-col gap-3 border-t border-slate-200 pt-8 text-sm text-slate-500 sm:flex-row sm:items-center sm:justify-between">
          <p>© <span id="year"></span> Naafiun. সর্বস্বত্ব সংরক্ষিত।</p>
          <p>ইসলামিক বই ও গিফট শপ</p>
        </div>
      </div>
    </footer>

    <!-- Cart Slide-out Panel -->
    <div id="cartOverlay" class="fixed inset-0 z-[60] hidden bg-slate-900/50 opacity-0 transition-opacity duration-300"></div>
    <div id="cartPanel" class="fixed inset-y-0 right-0 z-[70] flex w-full max-w-md translate-x-full flex-col bg-white shadow-2xl transition-transform duration-300 ease-out">
      <div class="flex items-center justify-between border-b border-slate-200 px-6 py-4">
        <div>
          <h2 class="text-lg font-bold text-slate-900">শপিং কার্ট</h2>
          <p class="mt-0.5 text-xs text-slate-500" id="cartItemCount">০টি আইটেম</p>
        </div>
        <button id="cartCloseBtn" type="button" class="rounded-xl p-2 text-slate-400 hover:bg-slate-100 hover:text-slate-600 transition" aria-label="Close cart">
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none">
            <path d="M6 18L18 6M6 6l12 12" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
          </svg>
        </button>
      </div>
      <div class="flex-1 overflow-y-auto px-6 py-4">
        <div id="cartEmptyState" class="hidden flex-col items-center justify-center py-12 text-center">
          <svg class="h-16 w-16 text-slate-300" viewBox="0 0 24 24" fill="none">
            <path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4H6Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M3 6h18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            <path d="M16 10a4 4 0 01-8 0" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
          </svg>
          <p class="mt-4 text-sm font-semibold text-slate-900">আপনার কার্ট খালি</p>
          <p class="mt-1 text-xs text-slate-500">কিছু বই যোগ করুন!</p>
          <button onclick="window.NaafiunCart.closeCartPanel()" type="button" class="mt-4 rounded-xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white hover:bg-emerald-700 transition">
            কেনাকাটা চালু রাখুন
          </button>
        </div>
        <div id="cartItemsContainer" class="flex flex-col gap-3"></div>
      </div>
      <div class="border-t border-slate-200 px-6 py-4">
        <div class="flex items-center justify-between">
          <p class="text-sm font-semibold text-slate-700">মোট</p>
          <p class="text-xl font-black text-slate-900" id="cartTotal">৳ ০</p>
        </div>
        <p class="mt-1 text-xs text-slate-500">ডেলিভারি চার্জ আলাদা</p>
        <a href="checkout" id="cartCheckoutBtn" type="button" class="mt-4 block w-full rounded-xl bg-emerald-600 px-5 py-3 text-center text-sm font-bold text-white shadow-sm transition hover:bg-emerald-700 disabled:opacity-50 disabled:cursor-not-allowed">
          চেকআউট করুন
        </a>
      </div>
    </div>

    <!-- Toast Container -->
    <div id="toastContainer" class="fixed bottom-4 right-4 z-[80] flex flex-col gap-2"></div>

    <script>
      // ===== Cart Panel Close Fix =====
      const cartCloseBtn = document.getElementById('cartCloseBtn');
      const cartOverlay = document.getElementById('cartOverlay');
      if (cartCloseBtn) {
        cartCloseBtn.addEventListener('click', () => {
          if (window.NaafiunCart && window.NaafiunCart.closeCartPanel) {
            window.NaafiunCart.closeCartPanel();
          }
        });
      }
      if (cartOverlay) {
        cartOverlay.addEventListener('click', () => {
          if (window.NaafiunCart && window.NaafiunCart.closeCartPanel) {
            window.NaafiunCart.closeCartPanel();
          }
        });
      }

      // ===== Mobile Header Interactions =====

      // Search toggle
      const searchBtn = document.getElementById('mobileSearchBtn');
      const searchBar = document.getElementById('mobileSearchBar');
      if (searchBtn && searchBar) {
        searchBtn.addEventListener('click', () => {
          const isOpen = !searchBar.classList.contains('hidden');
          // Close category bar if open
          const catBar = document.getElementById('mobileCatBar');
          if (catBar && !catBar.classList.contains('hidden')) catBar.classList.add('hidden');
          // Toggle search
          searchBar.classList.toggle('hidden');
          searchBtn.setAttribute('aria-expanded', String(!isOpen));
          if (!isOpen) {
            const input = searchBar.querySelector('input');
            if (input) setTimeout(() => input.focus(), 100);
          }
        });
      }

      // Category toggle
      const catBtn = document.getElementById('mobileCatBtn');
      const catBar = document.getElementById('mobileCatBar');
      if (catBtn && catBar) {
        catBtn.addEventListener('click', () => {
          const isOpen = !catBar.classList.contains('hidden');
          // Close search bar if open
          if (searchBar && !searchBar.classList.contains('hidden')) searchBar.classList.add('hidden');
          // Toggle category
          catBar.classList.toggle('hidden');
          catBtn.setAttribute('aria-expanded', String(!isOpen));
        });
      }

      // Profile dropdown
      const profileBtn = document.getElementById('mobileProfileBtn');
      const profileDropdown = document.getElementById('profileDropdown');
      if (profileBtn && profileDropdown) {
        profileBtn.addEventListener('click', (e) => {
          e.stopPropagation();
          const isOpen = !profileDropdown.classList.contains('hidden');
          profileDropdown.classList.toggle('hidden');
          profileBtn.setAttribute('aria-expanded', String(!isOpen));
        });
        // Close dropdown when clicking outside
        document.addEventListener('click', (e) => {
          if (!profileDropdown.contains(e.target) && e.target !== profileBtn) {
            profileDropdown.classList.add('hidden');
            profileBtn.setAttribute('aria-expanded', 'false');
          }
        });
      }

      // Close all panels when clicking outside
      document.addEventListener('click', (e) => {
        if (searchBtn && searchBar && !searchBtn.contains(e.target) && !searchBar.contains(e.target)) {
          searchBar.classList.add('hidden');
        }
        if (catBtn && catBar && !catBtn.contains(e.target) && !catBar.contains(e.target)) {
          catBar.classList.add('hidden');
        }
      });

      // ===== SIMULATE LOGIN STATE (remove this in production) =====
      // To test logged-in state, uncomment the block above or call:
      //   window.showLoggedIn('Your Name', 'your@email.com', 'avatar-url.png');
      // To go back to logged-out state, call:
      //   window.showLoggedOut();

      window.showLoggedIn = function(name, email, avatarUrl) {
        const iconGuest = document.getElementById('profileIconGuest');
        const avatarMobile = document.getElementById('profileAvatarMobile');
        const avatarDropdown = document.getElementById('dropdownAvatar');
        const loggedOut = document.getElementById('profileLoggedOut');
        const loggedIn = document.getElementById('profileLoggedIn');
        const dName = document.getElementById('dropdownName');
        const dEmail = document.getElementById('dropdownEmail');

        if (iconGuest) iconGuest.classList.add('hidden');
        if (avatarMobile) { avatarMobile.classList.remove('hidden'); avatarMobile.src = avatarUrl || ''; }
        if (avatarDropdown) { avatarDropdown.classList.remove('hidden'); avatarDropdown.src = avatarUrl || ''; }
        if (loggedOut) loggedOut.classList.add('hidden');
        if (loggedIn) loggedIn.classList.remove('hidden');
        if (dName && name) dName.textContent = name;
        if (dEmail && email) dEmail.textContent = email;
      };

      window.showLoggedOut = function() {
        const iconGuest = document.getElementById('profileIconGuest');
        const avatarMobile = document.getElementById('profileAvatarMobile');
        const avatarDropdown = document.getElementById('dropdownAvatar');
        const loggedOut = document.getElementById('profileLoggedOut');
        const loggedIn = document.getElementById('profileLoggedIn');

        if (iconGuest) iconGuest.classList.remove('hidden');
        if (avatarMobile) avatarMobile.classList.add('hidden');
        if (avatarDropdown) avatarDropdown.classList.add('hidden');
        if (loggedOut) loggedOut.classList.remove('hidden');
        if (loggedIn) loggedIn.classList.add('hidden');
      };

      // Hero slider
      const track = document.getElementById("heroTrack");
      const prev = document.getElementById("heroPrev");
      const next = document.getElementById("heroNext");
      const dots = Array.from(document.querySelectorAll(".heroDot"));
      if (track && dots.length) {
        let index = 0;
        const slideCount = dots.length;
        const setActive = (i) => {
          index = (i + slideCount) % slideCount;
          track.style.transform = `translateX(-${index * 100}%)`;
          dots.forEach((d, di) => {
            d.classList.toggle("bg-emerald-600", di === index);
            d.classList.toggle("bg-slate-300", di !== index);
            d.classList.toggle("w-6", di === index);
            d.classList.toggle("w-2.5", di !== index);
          });
        };
        const goNext = () => setActive(index + 1);
        prev?.addEventListener("click", () => setActive(index - 1));
        next?.addEventListener("click", goNext);
        dots.forEach((d) => d.addEventListener("click", () => setActive(Number(d.dataset.slide || 0))));
        let timer = setInterval(goNext, 4500);
        track.parentElement?.addEventListener("mouseenter", () => clearInterval(timer));
        track.parentElement?.addEventListener("mouseleave", () => (timer = setInterval(goNext, 4500)));
        setActive(0);
      }

      // Footer year
      const year = document.getElementById("year");
      if (year) year.textContent = String(new Date().getFullYear());

      // Copy link button
      const copyBtn = document.getElementById("copyLinkBtn");
      if (copyBtn) {
        copyBtn.addEventListener("click", () => {
          navigator.clipboard.writeText(window.location.href).then(() => {
            const orig = copyBtn.innerHTML;
            copyBtn.innerHTML = '<svg class="h-4 w-4" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path d="M9 12l2 2 4-4"/></svg>';
            copyBtn.classList.add("text-emerald-600", "border-emerald-600");
            setTimeout(() => { copyBtn.innerHTML = orig; copyBtn.classList.remove("text-emerald-600", "border-emerald-600"); }, 1500);
          });
        });
      }
    </script>

    <!-- Product detail page JS -->
    <?php if ($pg === 'details'): ?>
    <script>
    (function () {
      const params = new URLSearchParams(window.location.search);
      const productId = params.get("id");
      const productSlug = params.get("slug");
      window._currentProductId = null;

      async function loadProduct() {
        try {
          const res = await fetch("data/products.json");
          if (!res.ok) throw new Error("Failed to load products.json");
          const data = await res.json();
          const products = data.products || [];
          window._productsCache = products;

          let product = null;
          if (productId) product = products.find(p => p.id === parseInt(productId, 10));
          else if (productSlug) product = products.find(p => p.slug === productSlug);
          if (!product) product = products[0];

          window._currentProductId = product.id;

          // Breadcrumbs
          const bcCat = document.getElementById("breadcrumb-category");
          const bcTitle = document.getElementById("breadcrumb-title");
          if (bcCat) bcCat.textContent = product.category;
          if (bcTitle) bcTitle.textContent = product.title;
          document.title = "Naafiun — " + product.title;

          // Image
          const mainImg = document.getElementById("main-image");
          if (mainImg) mainImg.src = (product.images && product.images.length) ? product.images[0] : product.image;

          const thumbs = document.querySelectorAll("#gallery-thumbnails .thumbnail-img");
          const thumbBtns = document.querySelectorAll("#gallery-thumbnails button");
          if (product.images) {
            thumbs.forEach((img, i) => {
              if (product.images[i]) img.src = product.images[i];
              else img.closest("button").style.display = "none";
            });
          }
          thumbBtns.forEach((b, i) => {
            b.addEventListener("click", () => {
              const tImg = b.querySelector("img");
              if (tImg && tImg.src) {
                mainImg.src = tImg.src;
                thumbBtns.forEach(x => { x.classList.remove("border-emerald-600","border-2"); x.classList.add("border","border-slate-200"); x.classList.add("opacity-70"); });
                b.classList.remove("border","border-slate-200","opacity-70");
                b.classList.add("border-emerald-600","border-2");
              }
            });
          });

          // Meta
          const setTitle = document.getElementById("product-title");
          const setAuthor = document.getElementById("product-author");
          const setCat = document.getElementById("product-category");
          const setPub = document.getElementById("product-publisher");
          if (setTitle) setTitle.textContent = product.title;
          if (setAuthor) setAuthor.textContent = product.author;
          if (setCat) setCat.textContent = product.category;
          if (setPub) setPub.textContent = product.publisher;

          // Details
          const setPages = document.getElementById("detail-pages");
          const setLang = document.getElementById("detail-language");
          const setCover = document.getElementById("detail-cover");
          if (setPages) setPages.textContent = product.pages;
          if (setLang) setLang.textContent = product.language;
          if (setCover) setCover.textContent = product.format;

          // Description
          const setDesc = document.getElementById("product-description");
          if (setDesc) {
            const full = product.description || "";
            const shortD = full.length > 250 ? full.substring(0, 250) + "..." : full;
            let html = `<p>${shortD}</p>`;
            if (full.length > 250) {
              html += `<details class="mt-1"><summary class="cursor-pointer text-sm font-semibold text-emerald-600 hover:text-emerald-700 select-none">আরো পড়ুন</summary><p class="mt-2">${full.substring(250)}</p></details>`;
            }
            setDesc.innerHTML = html;
          }

          // Price
          const setPrice = document.getElementById("product-price");
          const setOrig = document.getElementById("product-original-price");
          const setBadge = document.getElementById("discount-badge");
          if (setPrice) setPrice.textContent = "৳ " + product.price.toLocaleString("bn-BD");
          if (setOrig) setOrig.textContent = "৳ " + product.originalPrice.toLocaleString("bn-BD");
          if (setBadge) setBadge.textContent = product.discount + "% ছাড়";

          // Related products
          loadRelated(product, products);

        } catch (err) {
          console.error("[Product] Error:", err);
        }
      }

      function loadRelated(current, all) {
        let related = all.filter(p => p.categoryEn === current.categoryEn && p.id !== current.id);
        if (related.length === 0) related = all.filter(p => p.id !== current.id);
        related = related.slice(0, 8);

        // Desktop sidebar
        const deskContainer = document.getElementById("related-products");
        if (deskContainer) {
          deskContainer.innerHTML = related.map(p => `
            <a href="<?php echo domain; ?>details?slug=${p.slug}" class="flex gap-2.5 rounded-lg border border-slate-200 bg-white p-2 transition hover:shadow-sm">
              <div class="h-16 w-12 shrink-0 overflow-hidden rounded bg-slate-100">
                <img src="${p.image}" alt="${p.title}" class="h-full w-full object-cover" loading="lazy" />
              </div>
              <div class="flex min-w-0 flex-1 flex-col justify-between py-0.5">
                <h3 class="line-clamp-2 text-xs font-semibold text-slate-900 leading-tight">${p.title}</h3>
                <div class="mt-1">
                  <span class="text-xs font-bold text-slate-900">৳ ${p.price.toLocaleString("bn-BD")}</span>
                  <span class="ml-1 text-[10px] text-slate-400 line-through">৳ ${p.originalPrice.toLocaleString("bn-BD")}</span>
                </div>
              </div>
            </a>
          `).join("");
        }

        // Mobile grid
        const mobContainer = document.getElementById("related-products-mobile");
        if (mobContainer) {
          mobContainer.innerHTML = related.map(p => `
            <a href="<?php echo domain; ?>details?slug=${p.slug}" class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm hover:shadow-md transition">
              <div class="aspect-[3/4] bg-slate-100">
                <img src="${p.image}" alt="${p.title}" class="h-full w-full object-cover" loading="lazy" />
              </div>
              <div class="p-2.5">
                <h3 class="line-clamp-2 text-xs font-semibold text-slate-900">${p.title}</h3>
                <div class="mt-1.5 flex items-baseline gap-1.5">
                  <span class="text-sm font-bold text-slate-900">৳ ${p.price.toLocaleString("bn-BD")}</span>
                  <span class="text-xs text-slate-400 line-through">৳ ${p.originalPrice.toLocaleString("bn-BD")}</span>
                </div>
              </div>
            </a>
          `).join("");
        }
      }

      if (document.readyState === "loading") {
        document.addEventListener("DOMContentLoaded", loadProduct);
      } else {
        loadProduct();
      }
    })();
    </script>
    <?php endif; ?>

    <!-- Cart system -->
    <script src="js/cart.js" defer></script>
    <!-- Home page product renderer -->
    <?php if ($pg === 'home'): ?>
    <script src="js/main.js" defer></script>
    <?php endif; ?>

    <!-- ===== FIXED BOTTOM NAV (Mobile Only) ===== -->
    <nav class="bottom-nav fixed bottom-0 left-0 right-0 z-50 flex items-center justify-around bg-white border-t border-slate-200 shadow-[0_-2px_10px_rgba(0,0,0,0.08)] sm:hidden">
      <!-- Home -->
      <a href="<?php echo domain; ?>" class="flex flex-col items-center justify-center py-2 px-3 min-w-0 transition active:scale-95">
        <svg class="h-5 w-5 text-slate-500" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <path d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-4 0a1 1 0 01-1-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 01-1 1" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <span class="mt-0.5 text-[10px] font-medium text-slate-500">হোম</span>
      </a>
      <!-- Wishlist -->
      <a href="#" class="flex flex-col items-center justify-center py-2 px-3 min-w-0 transition active:scale-95">
        <svg class="h-5 w-5 text-slate-500" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
          <path d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <span class="mt-0.5 text-[10px] font-medium text-slate-500">উইশলিস্ট</span>
      </a>
      <!-- Cart -->
      <button class="cart-trigger relative flex flex-col items-center justify-center py-2 px-3 min-w-0 transition active:scale-95" type="button" aria-label="কার্ট">
        <svg class="h-5 w-5 text-slate-500" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
          <path d="M2.25 3h1.386c.51 0 .955.343 1.087.835l.383 1.437M7.5 14.25a3 3 0 0 0-3 3h15.75m-12.75-3h11.218c1.121-2.3 2.1-4.684 2.924-7.138a60.114 60.114 0 0 0-16.536-1.84M7.5 14.25 5.106 5.272M6 20.25a.75.75 0 1 1-1.5 0 .75.75 0 0 1 1.5 0Zm12.75 0a.75.75 0 1 1-1.5 0 .75.75 0 0 1 1.5 0Z" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <span class="mt-0.5 text-[10px] font-medium text-slate-500">কার্ট</span>
        <span class="cart-badge-count absolute right-1 top-0.5 grid h-4 w-4 place-items-center rounded-full bg-emerald-500 text-[9px] font-bold text-white">0</span>
      </button>
      <!-- Checkout -->
      <a href="#" class="flex flex-col items-center justify-center py-2 px-3 min-w-0 transition active:scale-95">
        <svg class="h-5 w-5 text-slate-500" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <path d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <span class="mt-0.5 text-[10px] font-medium text-slate-500">চেকআউট</span>
      </a>
    </nav>

    <script>
      // Add body padding for bottom nav on mobile
      if (window.innerWidth < 640) {
        document.body.classList.add('has-bottom-nav');
      }
      window.addEventListener('resize', function() {
        document.body.classList.toggle('has-bottom-nav', window.innerWidth < 640);
      });
    </script>
  </body>
</html>
